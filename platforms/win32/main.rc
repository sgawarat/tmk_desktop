#include "resource.h"

IDR_CONTEXT_MENU MENU
BEGIN
    POPUP "Context Menu"
    BEGIN
        MENUITEM "Enable/Disable", ID_ENABLE_DISABLE
        MENUITEM "Release all keys", ID_RELEASE_ALL_KEYS
        MENUITEM "&Exit", ID_EXIT
    END
END
